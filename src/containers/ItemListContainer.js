const ItemListContainer = (prop) => {
    return (
        <div className="greeting-container">
            <h1 className="greeting">{prop.greeting}</h1>
        </div>
    )
}

export default ItemListContainer;